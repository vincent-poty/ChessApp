package org.chess.demo;

public class Knight extends Piece{
    public Knight(String name, int x, int y, boolean isblack, boolean isSelected, boolean isCaptured) {
        super(name, x, y, isblack, isSelected, isCaptured);
    }
}
