package org.chess.demo;

public class Rook extends Piece{
    public Rook(String name, int x, int y, boolean isblack, boolean isSelected, boolean isCaptured) {
        super(name, x, y, isblack, isSelected, isCaptured);
    }
}
