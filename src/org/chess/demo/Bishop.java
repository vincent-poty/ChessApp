package org.chess.demo;

public class Bishop extends Piece{
    public Bishop(String name, int x, int y, boolean isblack, boolean isSelected, boolean isCaptured) {
        super(name, x, y, isblack, isSelected, isCaptured);
    }
}
